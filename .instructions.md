---
name: TryHard Forge
description: Cursor instructions for Hebbian Persona Forge daily training and agent orchestration.
applyTo:
  - "tryhard-forge-trading/**"
---

When working in this repo:

1. Read `CURSOR_TRAINING.md` and `FORGE_RULES.md` first.
2. **Execute** forge scripts on the laptop — never only paste commands for the user.
3. Phase 1 (months 1–2): all memory nodes must be `HUMAN_CURATED`; sim only.
4. Daily curation: append `regime_library/daily_manifest.jsonl` → `python cursor_training_loop.py`.
5. Live MES gate: `readiness_checker.py` must return `APPROVED_FOR_MICRO_LIVE`.
6. GFLBS signals: JSON only; prompt from `JanusEngine/GFLBS/AGENT_SYSTEM_PROMPT.md`.
7. Grok: `XAI_API_KEY`; fallback Ollama `phi4:latest`.

Quick commands:
- `python cursor_training_loop.py`
- `python data_ingestion_patch.py --manifest regime_library/daily_manifest.jsonl`
- `python readiness_checker.py`
- `python run_trading_forge.py`